frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - amplifyPush --simple
    build:
      commands:
        - npx next telemetry disable
        - next build && next export
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
