frontend:
  phases:
    preBuild:
      commands:
        - amplifyPush --simple
        - npx next telemetry disable
        - yarn install
    build:
      commands:
        - yarn run export
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
