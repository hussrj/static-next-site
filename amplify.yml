frontend:
  phases:
    preBuild:
      commands:
        - amplifyPush --simple
        - npm ci
        - npx next telemetry disable
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
