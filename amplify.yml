frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - amplifyPush --simple
    build:
      commands:
        - npx next telemetry disable
        - npm run build
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
