frontend:
  phases:
    preBuild:
      commands:
        - amplifyPush --simple
        - yarn install
    build:
      commands:
        - yarn run export
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
